namespace ThunderbirdMsgFilterCopy.Core.Models;

/// <summary>
/// SPEC §3.2 / §4.2. Serialized to manifest.json exactly as these names.
/// </summary>
public enum AccountType
{
    Imap,
    Pop,
    Local,
}
